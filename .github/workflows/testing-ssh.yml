name: Testing SSH Access
on: workflow_dispatch
jobs:
  test:
    runs-on: ubuntu-latest
    steps: 
      - name: Connect via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: "172.20.71.180"
          username: "keicee"
          password: "kelechukwu1$"
          key: ${{ secrets.SSH_KEY }}
          debug: true
          script: touch ~/github-file.txt
